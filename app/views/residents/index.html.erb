<title>
Residents
</title>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link active" aria-current="page" href="#">Residents</a>
                <a class="nav-link" href="/food_types">FoodTypes</a>
            </div>
        </div>
    </div>
</nav>

<h1>
Residents
</h1>

<table class="table">
    <thead>
        <tr>
            <th>氏名</th>
            <th>ヨミガナ</th>
            <th>性別</th>
            <th>生年月日</th>
            <th>入居日</th>
        </tr>
    </thead>

    <tbody>
        <% @residents && @residents.each do |resident| %>
            <tr>
                <td><%= resident.name %></td>
                <td><%= resident.kana %></td>
                <td><%= resident.get_gender_str %></td>
                <td><%= resident.birthday.strftime('%Y-%m-%d') %></td>
                <td><%= resident.move_in_date.strftime('%Y-%m-%d %k時') %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<button type="button" class="btn btn-primary">追加する</button>