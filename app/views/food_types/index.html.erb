<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/">Residents</a>
        <a class="nav-link active" aria-current="page" href="#">FoodTypes</a>
      </div>
    </div>
  </div>
</nav>
<h1>
  FoodTypes
</h1>
<table class="table">
  <thead>
    <tr>
      <th>食形態</th>
      <th>色</th>
    </tr>
  </thead>
  <tbody>
    <% @food_types && @food_types.each do |food_type| %>
      <tr style="background-color: <%= food_type.color %>">
        <td><%= food_type.name %></td>
        <td><%= food_type.color %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<a href="/food_types/new">
<button type="button" class="btn btn-primary">追加する</button>
</a>
